{
  "name": "chrome-cross-storage",
  "version": "1.0.0",
  "description": "Google chrome storage and localStorage wrapper which help users debug google chrome application which depends on the storage as simple web apps",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "format": "prettier --write \"src/**/*.ts\" \"src/**/*.js\"",
    "lint": "tslint -p tsconfig.json",
    "test": "jest --config jestconfig.json",
    "coverage": "jest --coverage --config jestconfig.json"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "@types/chrome": "0.0.88",
    "typescript": "^3.5.3"
  },
  "devDependencies": {
    "@types/jest": "^24.0.17",
    "jest": "^24.8.0",
    "jest-localstorage-mock": "^2.4.0",
    "prettier": "^1.18.2",
    "ts-jest": "^24.0.2",
    "tslint": "^5.18.0",
    "tslint-config-prettier": "^1.18.0"
  },
  "files": [
    "lib/**/*"
  ]
}